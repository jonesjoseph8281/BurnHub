import React from 'react';

const OAuthRedirect = () => {
    return (
        <div>
            <h1>OAuth Redirect Page</h1>
            <p>You have been redirected from Google sign-in.</p>
            {/* Handle token exchange or user information retrieval here */}
        </div>
    );
};

export default OAuthRedirect;
